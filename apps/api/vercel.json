{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && cd prisma && npx prisma generate --schema=./schema.prisma && cd ../apps/api && pnpm --filter @celhm/types build && pnpm build",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}


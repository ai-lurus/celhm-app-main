{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @celhm/types build && pnpm --filter @celhm/api build",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}


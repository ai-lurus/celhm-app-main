# Database
DATABASE_URL=postgresql://<user>:<pass>@<host>:5432/<db>
SHADOW_DATABASE_URL=postgresql://<user>:<pass>@<host>:5432/<shadowdb>

# Auth (Supabase)
NEXT_PUBLIC_SUPABASE_URL=// TODO(ENV)
NEXT_PUBLIC_SUPABASE_ANON_KEY=// TODO(ENV)
SUPABASE_SERVICE_ROLE_KEY=// TODO(ENV)

# Notifications (commented until we have keys)
# RESEND_API_KEY=// TODO(ENV)
# TWILIO_ACCOUNT_SID=// TODO(ENV)
# TWILIO_AUTH_TOKEN=// TODO(ENV)
# WHATSAPP_PHONE_NUMBER_ID=// TODO(ENV)
# META_APP_TOKEN=// TODO(ENV)

# Redis (optional)
REDIS_URL=redis://localhost:6379

# Feature Flags (removed - no longer using mocks)

# API
API_PORT=3001
API_URL=http://localhost:3001

# Web
NEXT_PUBLIC_API_URL=http://localhost:3001

